(executable
  (name bundle_png)
  (modules bundle_png)
  (libraries str imagelib))

(executable
  (name incl_png)
  (modules incl_png)
  (libraries str))

(executable
  (name gen_html)
  (modules gen_html)
  (preprocess (pps tyxml-ppx))
  (libraries tyxml))

