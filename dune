
(env (dev (flags :standard -warn-error -32-34-37)))

(rule
	(targets images_coords.ml all.png)
  (deps (:gen gen/bundle_png.exe) (glob_files images/*))
	(action (with-stdout-to images_coords.ml (chdir %{project_root} (run %{gen})))))

